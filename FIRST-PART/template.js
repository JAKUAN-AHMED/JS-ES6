//first example
const first='jakuan';
const mid='ahmed';
const last='numan';
const FullName=first+' '+mid+' '+last;
const full=`${first} ${mid} ${last}`;
console.log(full);


//second example
const student=[34,343,43];
const name={first:'jakuan',age:30};
const result=`sum of ${student[0]} and ${name.age} is ${student[0]+name.age}`;
console.log(result);
